# AWS Credentials Template for EKS Deployment
# Copy this file to aws-credentials.txt and fill with your sandbox credentials
# Note: This file should NEVER be committed to git

[default]
aws_access_key_id = YOUR_ACCESS_KEY_HERE
aws_secret_access_key = YOUR_SECRET_ACCESS_KEY_HERE  
aws_session_token = YOUR_SESSION_TOKEN_HERE
region = us-east-1

# Instructions:
# 1. Copy this file: cp aws-credentials-template.txt aws-credentials.txt
# 2. Replace YOUR_*_HERE with your AWS sandbox credentials
# 3. Run: export AWS_SHARED_CREDENTIALS_FILE=$(pwd)/aws-credentials.txt
# 4. Proceed with deployment using ./deploy-aws-eks.sh

# Security Note:
# - aws-credentials.txt is in .gitignore to prevent accidental commits
# - Only use sandbox/temporary credentials
# - Never commit real AWS credentials to version control